# RESUME carp24 — 21.08.2026 (Tagesabschluss)

## Stand: ALPHA-READY + WARTUNGSMODUS + PWA + PLAYBOOK v1.1 (18:40)
Alles gebaut, verifiziert, committed. Migrationen 0016–0031 live. Link-Test 57/57 grün (ein+ausgeloggt). Philipp-Login: info@einfach-online.dev (ADMIN + Pro, PW in /tmp/carp24_philipp_pw.txt).

## Heute NACH 16:40 erledigt (chronologisch)
1. **Wartungsmodus KOMPLETT** (committed): Migration 0031 `app_settings` (key/value jsonb, RLS SELECT anon+auth) · `web/src/lib/settings.ts` (getMaintenance, 5s-Cache) · `web/src/pages/wartung.astro` (503, i18n, Custom-Message, Impressum/Datenschutz-Links) · `web/src/pages/api/admin/settings.ts` (GET+PATCH, Guard, Audit-Log) · Admin-Übersicht: Wartungs-Karte (Badge/Textarea/Toggle) · **Guard in `web/src/middleware.ts`** — E2E-Zyklus grün (anonym→/wartung 503, Admin kommt durch, /login+/impressum+/datenschutz erreichbar, Toggle an/aus, API 503-JSON)
   - **3 Lehren (wichtig für künftige Guards):** (a) `return Astro.redirect()` funktioniert NICHT in Layout-Komponenten (lässt Requests hängen) → Middleware nutzen; (b) Astro-7-Middleware-Cookies haben KEIN `getAll()` → Cookie-Lesen über `context.request.headers.get("cookie")` parsen (Muster aus .astro-Seiten); (c) Pfad-Normalisierung `pathname.replace(/\/+$/, "")` nötig (sonst matchen `/login/` vs `/login` nicht)
2. **Admin-Playbook v1.1** (LaTeX/PDF, 14 Seiten, EO-gebrandet, EO-ADMIN-2026-CARP24-01-REV): Wartungsmodus-Kapitel + Changelog — Vault + Repo committed. Datei: `docs/carp24-admin-playbook-v1.0.tex` (Titel intern v1.1)
3. **Cookie-Banner verifiziert** (KlaroBanner.astro, selbstgebaut — kein CDN): Banner sichtbar · kein Tracking vor Consent (Matomo `_paq` bekommt erst `setConsentGiven`) · „Alle akzeptieren"→localStorage=all + verschwindet · Reload persistiert · „Nur notwendige"→essential (kein Matomo) · min-h-11-Buttons ✅
4. **PWA-Manifest** (committed): `web/public/manifest.webmanifest` (standalone, Carp24-Farben #0B0C0B/#E2DECC/#6C7A57) + 3 Icons (icon-192/512 + maskable-512, aus favicon.svg generiert) + 5 Meta-Links in Layout.astro (theme-color, apple-touch-icon, manifest) — verifiziert 200. **Installierbar erst unter HTTPS (Beta-Domain CX22).** Store-Strategie beschlossen: PWA jetzt → Android TWA nach Beta (25$) → iOS nur bei Nachfrage (Capacitor, 99$/Jahr)

## Modell-Thema (KEINE Änderung heute — Config zurückgesetzt auf DeepSeek)
- **Problem:** DeepSeek hat API-Preise ab 16.08. massiv erhöht (V4-Flash Output $0.28 → bis $1.32 Peak = bis 1100%) → 8–10 $/Tag zu teuer
- **Recherche abgeschlossen:** MiMo-V2.5-Pro schlägt DeepSeek V4 Flash im agentic (Terminal-Bench 2.0 65,8 vs 56,9; SWE-Pro 56,1 vs 52,6), ist Marktführer bei Tool-Calls auf OpenRouter (122M = 18,6%), Preis $0,30/$0,61. **Hy3 (Tencent):** Tool-Calling ✅ (direkt/SiliconFlow — OpenRouter-Endpoint akzeptiert KEIN tools!), MCP-Atlas 79,1 (beste Tool-Orch.), $0,14/$0,56, aber 256K-Kontext
- **Entscheidung Philipp (21.08.):** MiMo-V2.5-Pro = Haupt, Hy3 = Fallback — **über DIREKT-Provider, Umstellung MORGEN NACHMITTAG (22.08.). OpenCode bleibt unverändert** (MiMo backend / GLM 5.2 frontend via OpenRouter)
- **Config heute:** zurückgesetzt auf `deepseek-v4-flash` / provider `deepseek` / `fallback_providers: []` — heute läuft alles wie bisher
- **Morgen nötig (Keys von Philipp):** Xiaomi-Direkt (MiMo) ODER SiliconFlow (Hy3/MiMo, Hermes wird dort explizit unterstützt) — dann `model.default` + `model.provider` + base_url + `fallback_providers` setzen
- **⚠️ Nach Modellwechsel: 7 Crons neu pinnen** (sonst fail closed): update-check (3653fa558fbf), vikunja-token-refresh (caa7501d75c7), Backup-Status Report (f0f308020d60), honcho-deep-check (9fa716e32049), 100-Tips-Compliance-Check (fdec465c1028), freellm-Watch (9a375296f11b), Server-Reboot-Erinnerung So 23.08. (25ba2156f0c1) — via `hermes cron edit <id> --provider <p> --model <m>`

## Offen für Alpha (nur Philipp)
1. OAuth-Creds (Google/FB) → infra/.env + auth-Recreate
2. Stripe-Keys (Test) → web/.env
3. OpenRouter-Key → web/.env (OPENROUTER_API_KEY — für Carp24-KI-Assistent, Kundendaten NUR via OpenRouter!)
4. CRON_KEY → web/.env + systemd-Timer 08:00 (daily-reminder)
5. Push-Browser-Test (Headless-Limit)
6. G1 Zoho-DPA (02_SYSTEM/AVV/README_ZOHO_DPA.md), G2 Matomo Site 4, G3 Rechtstexte 26.08. (Jurist)

## Nächster Schritt
**Philipp Alpha-Test auf http://100.93.250.103:8094** (Login info@einfach-online.dev). Danach H1–H8 → Beta auf CX22 (kein Timer). Morgen Nachmittag: Modell-Umstellung auf MiMo/Hy3 direkt.
