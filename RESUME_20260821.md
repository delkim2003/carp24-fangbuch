# RESUME carp24 — 21.08.2026 (Abend)

## Stand: ALLE 14 PRODUKT-FEATURES GEBaut+VERIFIZIERT — ALPHA-READY (15:20)
X1/X2 (P0) + A1–A4 + B1 + C1–C3 + D1–D2 + E1–E4 gebaut, verifiziert, committed (b997093…448b475). Vault FEATURE_MATRIX v4 (d5835c6).

### Infra-Delta (wichtig)
PUBLIC_SUPABASE_URL → http://100.93.250.103:8094 — Apache-Vhost carp24-build.conf proxyt /auth /rest /storage /realtime → Kong (100.93.250.103:8055). Grund: Realtime-WebSocket braucht gleichen Origin.

### Offen für Alpha (nur Philipp)
1. OAuth-Creds (Google/FB) → infra/.env + auth-Recreate
2. Stripe-Keys (Test) → web/.env (STRIPE_SECRET_KEY/WEBHOOK_SECRET/PRICE_ID + SUPABASE_SERVICE_ROLE_KEY)
3. OpenRouter-Key → web/.env (OPENROUTER_API_KEY)
4. Admin-Flag: nach Registrierung info@einfach-online.dev → role='admin'
5. Push-Browser-Test (Headless: kein serviceWorker)
6. G1 Zoho-DPA (02_SYSTEM/AVV/README_ZOHO_DPA.md), G2 Matomo Site 4, G3 Rechtstexte 26.08.

### Heute erledigt (chronologisch)
1. **Foto-Upload KOMPLETT gefixt** (`c244edb`): Bucket 0015 (privat+4 RLS), Canvas-EXIF-Strip, foto:-Pfad + signierte URLs, CSP blob:, UUID-Fallback; **Storage-Bug gelöst**: GLOBAL_S3_BUCKET=carp24-files (v1.60.4-TenantLocation-Bug; Downgrade v1.50.0 verworfen — Doku-Regel: einheitlicher Stack)
2. **deleted_at-Bug**: `.eq(x,null)` sendet String 'null' → `.is(x,null)` in **4 Seiten** (faenge, dashboard, profil, faenge/[id]) — Fangliste zeigte fälschlich „keine Fänge", Detail „nicht gefunden"
3. **Fehlende Tasks eingebaut**: 0.8 k6 (50.7k Req, 0% Fehler, p95 13ms), 0.5 Restore-DryRun (4 Backups valide), 0.12 CI-Basis, 0.13 ESLint/Prettier (0 Fehler), 0.14 Renovate
4. **Audit-Loop**: R1 (6 Funde, selbst statt Subagenten — v4-flash timeoutet) → Fixes → R2 (Fix-Verifikation + Regression-Scan) → **VERDICT PROCEED 88%** (`627543b`, Reports docs/AUDIT_RUNDE1+2)
5. **F3-Rest** (.error-Checks dashboard+Export) + **F5** (EXTERNAL_HOSTS) gefixt
6. **Temp-Watchdog endgültig** (`ce2a59c`): systemd-Timer alle 10 Min, killt Playwright-Leichen + Headless >80°C, GUI-Chrome sicher; 2 Beweistests grün; dezimal-Locale-Fix
7. **Gate-1-Checkliste** (Vault): 8/10 grün, GO

### Commits heute (Repo, neu→alt)
ce2a59c (Watchdog) · 627543b (Audit-R2) · f8aa0ad (ESLint) · 3378d6b (Restore-DryRun) · 299bfe1 (k6+CI+Renovate) · c244edb (Foto-Upload+deleted_at)
Vault: b7ab72d · 588a87f · 9f4deb0 (Feature-Matrix) — heute abend GATE1-CHECKLISTE folgt

### Offen für Philipp (Beta-Start)
- **G1 Zoho-DPA** signieren (Anleitung 02_SYSTEM/AVV/README_ZOHO_DPA.md) → PDF in 02_SYSTEM/AVV/
- **G2 Matomo Site 4** (1 Min manuell, Automation scheiterte 6×)
- **G3 Rechtstexte-Review** bis 26.08. (Jurist)
- G4 DNS beta.carp24.org + carp24.org → CX22-IP (vor Beta auf Hetzner; Alpha braucht kein DNS)

### Phasen-Kette (21.08., Philipp)
1. **Alpha:** Philipp testet auf Dev (:8094), Funde → Fixes
2. **Vor-Hetzner H1–H8** (DPA, Matomo Site 4, Rechtstexte, DNS, CX22-Setup, Security-Audit, Restore-Drill, Go-to-Market)
3. **Beta auf Hetzner (CX22):** Seed-User einladen (NUR echte Adressen — NXDOMAIN-Lehre!) + KPI-Loop
4. Kein Timer — CX22 kommt wenn H1–H8 grün

### Technische Fixes/Lehren (wichtig für neue Sessions)
- **Storage self-hosted**: GLOBAL_S3_BUCKET MUSS gesetzt sein (sonst InvalidKey "must be a relative path"); v1.60.4+ hat TenantLocation-Bug bei leerem Bucket — nicht einzeln downgraden (Doku-Regel MASTER_SPRINT: Stack einheitlich)
- **Null-Filter**: IMMER `.is("col", null)`, nie `.eq("col", null)` (PostgREST-String)
- **DB**: heißt `postgres`; auth.users via supabase_auth_admin, storage/error_logs via supabase_admin
- **Subagenten** (v4-flash) timeouten bei Voll-Audits → mechanische Checks selbst
- **Watchdog**: /var/log/temp-watchdog.log, Temp aktuell 27,8°C
