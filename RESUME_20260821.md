# 🔄 RESUME — Carp24 (Stand 21.08.2026, ~08:35 — Aufräum-Session)

> **Zweck:** Nahtloser Wiedereinstieg in einer NEUEN Session. Alles, um exakt hier weiterzumachen.
> **Projekt:** `/mnt/projekte/carp24-fangbuch/` · Branch `sprint-1` · Vault `03_PROJEKTE/carp24-fangbuch/BAUPLAN.md` (v4.5)

---

## ✅ WAS HEUTE FERTIG WURDE (Aufräumen: P3-Kosmetik + Karten-Pick + Git-Sauberkeit)

| Task | Inhalt | Beweis |
|------|--------|--------|
| **Git-Aufräumen** | Migrationen `0002_seed_waters.sql` + `0003_seed_catches.sql` + `supabase/functions/gewaesser-search/` committet (waren untracked → Restore-Sicherheit); `infra/volumes/functions/` in .gitignore | Commit `a343395` |
| **P3: CTA-Redundanz** | index.astro: „ANMELDEN" (→/login, dupliziert) → „FUNKTIONEN ENTDECKEN" (→/#funktionen, Anker an Feature-Section) | Commit `49d95f8` |
| **P3: min-h-Konsistenz** | index.astro „LOS GEHT'S" min-h-11 → min-h-12 (Primary-Button-Regel: Primary/Inputs = min-h-12, Chips/Secondary = min-h-11) | Commit `49d95f8` |
| **P3: Pflichtfeld-Sternchen** | fang-erfassen.astro: GEWICHT\* / FISCHART\* / DATUM\* | Commit `49d95f8` |
| **Karten-Pick (3. Geo-Methode)** | Leaflet 1.9.4 (npm), Button „AUF KARTE WÄHLEN", divIcon-Marker, Klick setzt lat/lng; **Klaro-Consent-Gate: OSM-Tiles nur bei „Alle akzeptieren", sonst Hinweis**; dynamischer Import (kein Bundle-Bloat) | Commit `49d95f8` + `8c52b53` (dep); **E2E live: Hinweis ohne Consent → Karte nach Consent (8 OSM-Tiles 200) → Klick → lat=47.509780 / lng=14.304199** |
| **🔴 P1-FIX: Client-Supabase tot** | `window.supabase` wurde NIE gesetzt (Git-Historie leer) → alle is:inline-Scripts (Login/Registrierung/Submit/Abmelden/Sync) scheiterten mit „Supabase nicht initialisiert". Fix: Layout setzt `window.supabase = { createClient: createBrowserClient }` | Commits `c0a447b`→`fadfc23`→`a635988`; **Login-E2E live: Redirect auf /fang-erfassen** |
| **🔴 P1-FIX: SSR rendert geschützte Seiten immer ausgeloggt** | Server-Client (supabase-client.ts) las keine Cookies → Auth-Guard + Frontmatter-Queries immer anon. Fix: `createServerClient` (@supabase/ssr) mit Astro.cookies in 6 geschützten Seiten (fang-erfassen, faenge, faenge/[id], dashboard, statistik, profil) | Commits `4182bab` + `2ff18bc` + `ba8a19a`; **E2E: /fang-erfassen rendert eingeloggt** |
| **🔴 P1-FIX: Astro.cookies.getAll existiert nicht** | Astro 7 API hat KEIN getAll (offiziell: get/has/set/delete/headers) → HTTP 500 beim Rendern. Fix: getAll via Cookie-Header-Parse (`Astro.request.headers.get("cookie")`) | Commit `2ff18bc`; 500 → 200 |
| **Session-Cookie-Sync** | supabase-js v2.39+ setzt im Browser KEINE Cookies mehr (nur localStorage) → SSR sah nie eine Session. Fix: `createBrowserClient` aus @supabase/ssr (sync Cookies ↔ localStorage) | Commit `a635988`; **Login → eingeloggtes SSR-Rendering** |

**Live:** http://100.93.250.103:8094/ · alle 8 Routen 200 · Service aktiv · Backup 02:30 gelaufen

---

## 🔜 NÄCHSTER TASK (BAUPLAN-Reihenfolge)

1. **Gate 1 abnehmen** (Philipp — Empfehlung: GO, 0 offene P0–P2) ODER direkt **1.16 E-Mail-Templates** (Willkommen/Passwort-Reset/Export/Lösch-Bestätigung; braucht D-5/Stitch-Design + SMTP-Mailer aus 1.9 — **SMTP-Status noch ungeprüft!**)
2. Danach Phase 1.5: **1.5.1a Datenexport (Art. 20) ist DSGVO-Pflicht VOR Beta-Usern** (hängt an 1.16) → Preflight 1.5.0 (DNS beta.carp24.org, Rechtstexte-Review D-5, SMTP, Matomo-Site 4)

## ⏳ OFFENE PUNKTE (dokumentiert, NICHT vergessen)

1. **🟠 SW-Update-Strategie fehlt** (NEU, Fund 21.08.): Service-Worker cached Assets cache-first, kein skipWaiting/claim → nach Deploys sehen Nutzer ALTES Bundle (hat die Debug-Session massiv verfälscht). Fix: SW-Update-Strategie (Cache-Versionierung oder clients.claim) VOR Beta. → BAUPLAN/Backlog
2. **🟠 Matomo.js wird von eigener CSP geblockt** (NEU, Fund 21.08.): `script-src 'self' 'unsafe-inline'` ohne matomo.einfach-online.dev → Tracking läuft nicht. Fix: CSP erweitern + Site 4 anlegen (WAITING_ON_ME #14)
3. **Restpunkte P3 (akzeptiert):** Startseiten-CTA ist behoben; min-h-Regel dokumentiert; Karten-Pick ✅ → P3-Kosmetik KOMPLETT
4. **Rechtstexte juristischer Review** (D-5-Frist) vor Private Beta
5. **Beta-Blocker:** Kong-Rate-Limit auf auth/rest, DISABLE_SIGNUP→true vor Beta, Top-25/50-Seed, Prod-Perf-Messung (Lighthouse ≥90 erwartet)
6. **FIX_PROMPT_*.txt (docs/) enthalten Secrets** (JWT_SECRET-Matches, OpenCode-Secret-Scan blockt) → NIE committen, bleiben untracked
7. Matomo-Site 4 (WAITING #14), Watercraft #1 aktiv, AEO Endspiel #12 + Game #13 frei

## 🧠 WICHTIGE LEHREN (neu seit letztem Resume)

- **supabase-js v2.39+ setzt im Browser KEINE Cookies mehr** — nur localStorage. Für SSR-Auth: @supabase/ssr createBrowserClient (Client, setzt Cookies) + createServerClient (Server, liest Cookies). Supabase-js direkt = SSR blind.
- **Astro 7 hat KEIN `Astro.cookies.getAll()`** — nur get/has/set/delete/merge/headers. Cookie-Header-Parse für getAll-Ersatz.
- **`window.supabase` global war NIE gesetzt** — is:inline-Scripts verlangen es seit 1.5b. Jede neue Seite mit is:inline-Supabase nutzt dieses Pattern.
- **Secret-Scan-Blockade (OpenCode):** docs/FIX_PROMPT_*.txt mit JWT_SECRET-Matches blocken `git add -A`-Commits → gezielt nur geänderte Dateien stagen.
- OpenCode + präziser Prompt (komplettes Code-Gerüst) = <3 Min Builds, 0 Analyse-Paralyse (MiMo etabliert).

## 🛠️ WICHTIGE KOMMANDOS

```bash
# Build + Deploy
cd /mnt/projekte/carp24-fangbuch/web && npm run build && sudo systemctl restart carp24-web

# OpenCode-Build (stdin, NIE als Argument — Shell-Escaping)
cat docs/BRIEFING_XXX.txt | opencode run --model "openrouter/xiaomi/mimo-v2.5-pro"

# E2E-Login (Supabase)
# e2e-1787225016@carp24.dev / TestPasswort123! (KEIN TOTP aktiv)
# API: http://100.93.250.103:8055 (Kong), Creds in /tmp/carp24_supabase_creds.txt + infra/.env

# DB
docker exec supabase-db psql -U supabase_admin -d postgres
```
