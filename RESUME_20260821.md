# 🔄 RESUME — Carp24 (Stand 21.08.2026, ~10:30 — Vor-Beta-Session)

> **Zweck:** Nahtloser Wiedereinstieg in einer NEUEN Session. Alles, um exakt hier weiterzumachen.
> **Projekt:** `/mnt/projekte/carp24-fangbuch/` · Branch `sprint-1` · Vault `03_PROJEKTE/carp24-fangbuch/BAUPLAN.md` (v4.5)

---

## ✅ WAS HEUTE FERTIG WURDE (Vor-Beta-Paket 21.08.)

| Task | Inhalt | Beweis |
|------|--------|--------|
| **Aufräumen + P3 + Karten-Pick** | Git (Migrationen/Edge-Functions committet), CTA-Redundanz, min-h-12, Pflichtfeld-Sternchen, Leaflet-Karten-Pick mit Klaro-Gate | Commits `a343395`/`49d95f8`/`8c52b53`; E2E lat=47.509780/lng=14.304199 |
| **🔴 P1: Login/SSR-Kette** | window.supabase global (createBrowserClient), createServerClient + Cookie-Header-Parse in 6 geschützten Seiten, Astro-7-API (KEIN getAll) | Commits `c0a447b`→`a635988`; Login-E2E → eingeloggtes SSR-Rendering |
| **1.5.1a Datenexport (Art. 20) ✅** | DSGVO-Sektion im Profil: JSON (user+profile+ALLE Fänge inkl. Drafts) + CSV (Semikolon, BOM, Vienna-Datum), Download im Browser, kein SMTP nötig | Commit `758f24e`; **E2E: Login → JSON mit 27 echten Fängen + CSV (28 Zeilen)** |
| **supabase-client vereinheitlicht ✅** | `createClient` → `createBrowserClient` in supabase-client.ts (Cookie-Session für gebündelte Scripts; fixte auch leere Profil/MFA-Daten) | Commit `758f24e` |
| **Matomo-CSP-Fix ✅** | Layout-CSP: script/connect-src + matomo.einfach-online.dev (Tracking-Code kann laden, sobald Site 4 existiert) | Commit `758f24e` |
| **SW-Update-Strategie ✅ (kein Fix nötig)** | sw.js hat bereits `skipWaiting` (install) + `clients.claim` (activate) + Cache-Cleanup → Fund abgehakt | sw.js geprüft |
| **Kong-Rate-Limit ✅ (abgedeckt)** | rate-limiting-Plugins in kong.yml vorhanden + GOTRUE_RATE_LIMIT_EMAIL_SENT aktiv | kong.yml |
| **1.16 E-Mail-Templates + SMTP ✅ KOMPLETT** | 7 GoTrue-HTML-Templates (Carp24-Branding) via templates-server (Caddy) + GOTRUE_MAILER_TEMPLATES/SUBJECTS; **SMTP via Zoho AKTIV** (smtp.zoho.eu:465, 235 Auth OK, kein Noop-Mailer) | Commits `ae26f4b` + Vault `12a3cc2`; **Beweis: Signup-Test `confirmation_sent_at` gesetzt + Test-Mail „Carp24 SMTP-Test OK" in INBOX angekommen (21.08. 09:29 CEST) + 49 alte Bounces aus Postfach aufgeräumt** |
| **Zoho-DPA vorbereitet** | Vault-Ordner `02_SYSTEM/AVV/` + offizielle Anleitung (README_ZOHO_DPA.md: accounts.zoho.eu → My Account → Privacy → Data Processing Addendum → Initiate Now) | Vault `9bcf818`; DSGVO_AVV.md auf Zoho umgestellt (MailerSend-Plan obsolet) |

**Live:** http://100.93.250.103:8094/ · alle Routen 200 · Service aktiv · Backup 02:30 läuft

---

## 🔜 NÄCHSTER TASK: Beta-Vorbereitung abschließen (Preflight 1.5.0)

1. **Philipp-Manual (~15 Min, 3 Punkte):**
   - **Zoho-DPA signieren** (Anleitung: Vault `02_SYSTEM/AVV/README_ZOHO_DPA.md`) → PDF im Vault ablegen
   - **Matomo-Site 4** (matomo.einfach-online.dev → Verwaltung → Webseiten → „ADD A NEW MEASURABLE": Carp24, https://carp24.org, Europe/Vienna) — Hermes-Automation scheiterte (Angular-SPA: API 401/kein Token, Add-Dialog öffnet nicht)
   - **DNS beta.carp24.org anlegen** (A-Record → 135.181.80.79; carp24.org existiert, beta fehlt)
2. **Philipp-Entscheidung: Gate 1 abnehmen** (Empfehlung: GO, 0 offene P0–P2) + **Rechtstexte-Review (D-5, Jurist)**
3. **Danach Hermes: 1.5.1 Beta-Deploy** (beta.carp24.org, nginx/TLS) → 1.5.2 Seed-User einladen (D-2) → 1.5.3 KPI-Loop

## ⏳ OFFENE PUNKTE (dokumentiert, NICHT vergessen)

1. **Zoho-DPA** — nur Philipp (Org-Admin); Pflicht vor Go-Live laut AVV-Checkliste (WAITING #15 Rest)
2. **Matomo-Site 4** — WAITING #14; Tracking-Snippet + CSP sind bereit, nur Site fehlt
3. **DNS beta.carp24.org** — fehlt (carp24.org → 135.181.80.79 existiert)
4. **Rechtstexte juristischer Review** (D-5-Frist) vor Private Beta
5. **Kong-Rate-Limit** — abgedeckt (Plugins + GOTRUE_RATE_LIMIT); DISABLE_SIGNUP bleibt OFFEN für Beta (User registrieren sich)
6. **OAuth-Dev-Konten (D-1)** — Philipp, für Beta-User-Optionen
7. **FIX_PROMPT_*.txt (docs/) enthalten Secrets** (JWT_SECRET-Matches) → NIE committen, bleiben untracked
8. Sonstige: Watercraft #1 aktiv, AEO Endspiel #12 + Game #13 frei (carp24 Vorrang abgearbeitet)

## 🧠 WICHTIGE LEHREN (neu seit letztem Resume)

- **supabase-js v2.39+ setzt im Browser KEINE Cookies mehr** — nur localStorage. Gebündelte Scripts müssen `createBrowserClient` (@supabase/ssr) nutzen, sonst sehen sie die Session nicht (Export/MFA-Daten leer).
- **SMTP-Tests NIE mit nicht-existierenden Domains** (@carp24.dev) → Bounce-Müll im Postfach. Nur echte Test-Adressen (info@einfach-online.dev).
- **Zoho-SMTP funktioniert mit denselben Creds für IMAP** (imap.zoho.eu:993) → Postfach-Diagnose (Zustellung/Bounce) direkt prüfbar.
- **Matomo 5.12 (Angular-SPA):** curl/Playwright-API-Zugriff braucht token_auth; Session-Cookies reichen für API nicht (401/„requires superuser"); Add-Dialog öffnet via `a.btn.addSite` NICHT automatisiert → manuell anlegen.
- **Matomo-DB liegt in `postgres`-DB** (nicht `_supabase`/`supabase`); `docker exec --env PGPASSWORD=… supabase-db psql -h localhost -U supabase_auth_admin -d postgres`.
- **Astro 7 hat KEIN `Astro.cookies.getAll()`**; `window.supabase` global = Modul `{ createClient }`.
- OpenCode + präziser Prompt (komplettes Code-Gerüst) = <3 Min Builds, 0 Analyse-Paralyse (MiMo etabliert).

## 🛠️ WICHTIGE KOMMANDOS

```bash
# Build + Deploy
cd /mnt/projekte/carp24-fangbuch/web && npm run build && sudo systemctl restart carp24-web

# OpenCode-Build (stdin, NIE als Argument — Shell-Escaping)
cat docs/BRIEFING_XXX.txt | opencode run --model "openrouter/xiaomi/mimo-v2.5-pro"

# E2E-Login (Supabase)
# e2e-1787225016@carp24.dev / TestPasswort123! (KEIN TOTP aktiv)
# API: http://100.93.250.103:8055 (Kong), Creds in infra/.env

# DB (auth.users liegt in postgres!)
PW=$(docker exec supabase-db printenv PGPASSWORD)
docker exec --env PGPASSWORD="$PW" supabase-db psql -h localhost -U supabase_auth_admin -d postgres

# Zoho-Postfach-Diagnose (IMAP)
# imap.zoho.eu:993, gleiche Creds wie SMTP; Bounces: FROM mailer-daemon@mail.zoho.eu
```
