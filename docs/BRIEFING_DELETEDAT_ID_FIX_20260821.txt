BAUE SOFORT — KEINE ANALYSE. Ein Fix in EINER Datei.

Datei: /mnt/projekte/carp24-fangbuch/web/src/pages/faenge/[id].astro
Zeile 42: `const { data, error } = await supabase.from("catches").select("*").eq("id", id).eq("deleted_at", null).single();`
→ ändere `.eq("deleted_at", null)` zu `.is("deleted_at", null)` (PostgREST-Null-Filter; .eq sendet den String 'null' → Postgres-Cast-Fehler → FANG NICHT GEFUNDEN).

NICHTS anderes ändern.

VERIFIKATION:
1. cd /mnt/projekte/carp24-fangbuch/web && npm run build → exit 0
2. grep -c '.is("deleted_at", null)' "web/src/pages/faenge/[id].astro" → 1
3. KEIN git commit.

AUSGABE kurz: Datei + Build-Ergebnis.
