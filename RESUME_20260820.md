# 🔄 RESUME — Carp24 Phase 1B (Stand 20.08.2026, ~18:25)

> **Zweck:** Nahtloser Wiedereinstieg in einer NEUEN Session. Alles, um exakt hier weiterzumachen.
> **Projekt:** `/mnt/projekte/carp24-fangbuch/` · Branch `sprint-1` · Vault `03_PROJEKTE/carp24-fangbuch/BAUPLAN.md` (v4.5)

---

## ✅ WAS FERTIG IST (Phase 1A + 1B-Teil)

### Backend (Phase 1A) — KOMPLETT, Gate 1A GO
- Migrationen 0001–0012 applied + committed; Stack 11/11 healthy
- Experten-QA (deleg_bb0e05d8): 5 Funde gefixt, Re-Audit 10/10 grün
- Edge-Functions live: `wetter-hook`, `gewaesser-search` (:8055)
- E2E-User: `e2e-1787225016@carp24.dev` / `TestPasswort123!` (19 publizierte Fänge)

### Frontend (Phase 1B) — 4 Tasks fertig
| Task | Inhalt | Commit |
|------|--------|--------|
| **1.3** | supabase-client.ts, @supabase/supabase-js, CSP, .env | `c9de493` |
| **1.5b** | Fang-Formular auf echte API: sync_catch/publish_catch, Fischart-Chips, GPS auto + manuelle lat/lng, Gewässer-Autocomplete (gewaesser-search) + Freitext, Foto-Upload, Auth-Guard, Login-Flow | `713c0e1` + `ceaa214` |
| **1.6b** | Offline-Draft-Queue: IndexedDB (offline-queue.ts), SyncIndicator, Offline-Submit, auto-sync | `bbf08f8` |
| **1.8** | Fangliste + Detail auf ECHTE Supabase-Daten: CatchCard, Empty-State, Success-Banner, Wetter-Snapshot (Mond+Luftdruck) | `63ade21` |

**Weitere Commits:** `5b47c7b` (DEPLOY_SSR.md), Briefings für 1.5b/1.6b/1.8

### Deploy (SEIT 1.8 SSR!)
- **WICHTIG:** Frontend ist seit 1.8 **SSR** (Astro Node-Adapter), NICHT statisch!
- Systemd-Service `carp24-web` (Node auf 127.0.0.1:4321, Env aus web/.env)
- Apache-VHost `carp24-build.conf` (Port 8094) → Reverse-Proxy auf 4321
- **Rebuild-Deploy:** `cd web && npm run build && sudo systemctl restart carp24-web`
- **Doku:** `docs/DEPLOY_SSR.md`
- **Live:** http://100.93.250.103:8094/ (alle 8 Routen 200, Fangliste zeigt echte DB-Fänge)

### BAUPLAN v4.5 (Vault)
- Experten-QA-Stops an ALLEN 7 Gates eingebaut (Lehre aus deleg_bb0e05d8)
- Gate 1A ✅ durchgeführt; Gate 0/1/1.5/2/3/4 ⏳ ausstehend
- Vault-Commit `983c5f9`

---

## 🔜 NÄCHSTER TASK: **1.10 Basis-Dashboard**
- `web/src/pages/dashboard.astro` ist NOCH MOCK (`const stats` Zeile 1, hartkodierte Beispieldaten!)
- DoD: Gewichts-Verlauf, Monats-Chart, Top-Fänge — aus ECHTEN Supabase-Daten (catches-Query wie in faenge.astro), **createClient-Grep ≥1**, kein Mock
- Danach: **1.10b Statistik-Studio-Frontend** (Bedingungs-UI auf 1.10a-API, Drill-down)

## ⏳ OFFENE PUNKTE (dokumentiert, NICHT vergessen)
1. **Karten-Pick** (3. Geo-Methode im Fang-Formular — Teil des 1.5b-DoD, noch nicht gebaut)
2. **Pro-Lock-Design-Frage:** grau vs. Gold (für 1.10b gebraucht — Philipp nicht beantwortet)
3. **Experten-QA-Stop vor Gate 1** steht an (sobald 1B-Kern fertig: Fangliste, Dashboard, Statistik-Studio, Rechtstexte)
4. **Rechtstexte 1.11** (Impressum + Datenschutz — Beta-Pflicht/DSGVO, D-5 Review-Frist)
5. **2FA/TOTP-UI 1.4a** (API aus 1.4 existiert, UI fehlt)
6. **404 + Offline-Seite 1.15** (SW-Fallback fehlt, kein service-worker.js)
7. Stitch-Projekt-Aufräumen: falsches Projekt 13734773594641617551 (delete blockiert)
8. Beta-Blocker: Kong-Rate-Limit auf auth/rest, DISABLE_SIGNUP→true vor Beta, Top-25/50-Seed

---

## 🧠 WICHTIGE LEHREN (neu seit letztem Resume)
- **`import.meta.env` wird in `is:inline`-Scripts NICHT ersetzt!** (Bundler-Problem) → Env zentral im Layout lesen, als `data-supabase-url`/`data-supabase-anon-key` am `<body>`, Scripts lesen per `document.body.dataset.supabaseUrl`. NIE `%PLATZHOLDER%` hartcodieren. (Doku: DEPLOY_SSR.md)
- **Dynamische Routen in Astro:** ohne `getStaticPaths` brauchen sie `export const prerender = false` + Node-Adapter (→ SSR-Deploy)
- **MiMo-Analyse-Paralyse:** Wenn OpenCode >10 Min nur liest ohne zu schreiben → Prozess killen + Prompt mit KOMPLETTEM Code-Gerüst neu starten („BAUE SOFORT — KEINE ANALYSE"). Funktioniert zuverlässig.
- **MiMo schreibt `%PLATZHOLDER%`** für Env-Vars statt import.meta.env → nach jedem Build `grep -rn "%PUBLIC_\|%VITE_" src/` prüfen!
- **OpenCode-Verifikation:** Kind-Prozess mit >30% CPU = arbeitet (NICHT killen, Output-Buffer zeigt nur nichts); 0% CPU an Socket = echt gehängt.

## 🛠️ WICHTIGE KOMMANDOS
```bash
# Build + Deploy
cd /mnt/projekte/carp24-fangbuch/web && npm run build && sudo systemctl restart carp24-web

# OpenCode-Build (Backend/Logik)
cat /tmp/prompt_xxx.txt | opencode run --model "openrouter/xiaomi/mimo-v2.5-pro"

# E2E-Login (Supabase)
# e2e-1787225016@carp24.dev / TestPasswort123!
# API: http://100.93.250.103:8055 (Kong), Creds in /tmp/carp24_supabase_creds.txt + infra/.env

# DB
docker exec supabase-db psql -U supabase_admin -d postgres

# Temperatur-Check (Chrome offen → >70°C)
sensors | grep "Package id 0"
```
