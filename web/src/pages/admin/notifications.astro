---
import Layout from "../../layouts/Layout.astro";
import { getSessionAndProfile } from "../../lib/admin";

const { session, isAdmin } = await getSessionAndProfile(Astro);
if (!session) return Astro.redirect("/login/");
if (!isAdmin) return Astro.redirect("/");
---

<Layout title="Carp24 – Admin – Benachrichtigungen" noindex>
  <main class="flex-grow flex flex-col items-center px-margin-mobile md:px-margin-desktop py-xl max-w-7xl mx-auto w-full">
    <div class="w-full mb-xl">
      <p class="font-label-sm text-label-sm uppercase tracking-widest text-primary mb-sm">PASSION. FISHING. COMMUNITY.</p>
      <h1 class="text-headline-lg text-on-surface">ADMIN-BENACHRICHTIGUNGEN</h1>
    </div>

    <nav class="flex gap-4 mb-xl flex-wrap">
      <a href="/admin/" class="font-nav-item text-nav-item uppercase tracking-widest px-4 py-2 rounded-full bg-surface-container-highest text-on-surface hover:bg-primary-container hover:text-on-primary-container transition-colors min-h-12 flex items-center">Übersicht</a>
      <a href="/admin/reports/" class="font-nav-item text-nav-item uppercase tracking-widest px-4 py-2 rounded-full bg-surface-container-highest text-on-surface hover:bg-primary-container hover:text-on-primary-container transition-colors min-h-12 flex items-center">Reports</a>
      <a href="/admin/users/" class="font-nav-item text-nav-item uppercase tracking-widest px-4 py-2 rounded-full bg-surface-container-highest text-on-surface hover:bg-primary-container hover:text-on-primary-container transition-colors min-h-12 flex items-center">Nutzer</a>
      <a href="/admin/content/" class="font-nav-item text-nav-item uppercase tracking-widest px-4 py-2 rounded-full bg-surface-container-highest text-on-surface hover:bg-primary-container hover:text-on-primary-container transition-colors min-h-12 flex items-center">Inhalte</a>
      <a href="/admin/revenue/" class="font-nav-item text-nav-item uppercase tracking-widest px-4 py-2 rounded-full bg-surface-container-highest text-on-surface hover:bg-primary-container hover:text-on-primary-container transition-colors min-h-12 flex items-center">Revenue</a>
      <a href="/admin/audit/" class="font-nav-item text-nav-item uppercase tracking-widest px-4 py-2 rounded-full bg-surface-container-highest text-on-surface hover:bg-primary-container hover:text-on-primary-container transition-colors min-h-12 flex items-center">Audit</a>
      <a href="/admin/notifications/" class="font-nav-item text-nav-item uppercase tracking-widest px-4 py-2 rounded-full bg-surface-container-highest text-on-surface hover:bg-primary-container hover:text-on-primary-container transition-colors min-h-12 flex items-center">Benachrichtigungen</a>
      <a href="/admin/settings/" class="font-nav-item text-nav-item uppercase tracking-widest px-4 py-2 rounded-full bg-surface-container-highest text-on-surface hover:bg-primary-container hover:text-on-primary-container transition-colors min-h-12 flex items-center">Einstellungen</a>
    </nav>

    <div class="w-full max-w-2xl">
      <div class="bg-surface-container-lowest rounded-2xl p-lg border border-outline/20">
        <h2 class="text-headline-md text-on-surface mb-lg">Push-Benachrichtigung senden</h2>

        <form id="notify-form" class="flex flex-col gap-lg">
          <!-- Title -->
          <div>
            <label for="title" class="font-label-sm text-label-sm text-on-surface block mb-xs">Titel</label>
            <input
              type="text"
              id="title"
              name="title"
              required
              maxlength="128"
              class="w-full px-4 py-3 rounded-xl bg-surface-container-highest text-on-surface border border-outline/20 focus:outline-none focus:border-primary placeholder:text-on-surface-variant"
              placeholder="z. B. Wartungsarbeiten"
            />
          </div>

          <!-- Body -->
          <div>
            <label for="body" class="font-label-sm text-label-sm text-on-surface block mb-xs">Nachricht</label>
            <textarea
              id="body"
              name="body"
              required
              maxlength="500"
              rows="4"
              class="w-full px-4 py-3 rounded-xl bg-surface-container-highest text-on-surface border border-outline/20 focus:outline-none focus:border-primary placeholder:text-on-surface-variant resize-none"
              placeholder="z. B. Die App ist heute Abend von 22:00–23:00 Uhr wegen Wartungsarbeiten nicht erreichbar."
            ></textarea>
          </div>

          <!-- Filter: Role -->
          <div>
            <label for="role-filter" class="font-label-sm text-label-sm text-on-surface block mb-xs">Empfänger-Rolle (optional)</label>
            <select
              id="role-filter"
              name="role-filter"
              class="w-full px-4 py-3 rounded-xl bg-surface-container-highest text-on-surface border border-outline/20 focus:outline-none focus:border-primary"
            >
              <option value="">Alle Rollen</option>
              <option value="USER">Nur USER</option>
              <option value="MODERATOR">Nur MODERATOR</option>
              <option value="ADMIN">Nur ADMIN</option>
            </select>
          </div>

          <!-- Filter: is_pro -->
          <div class="flex items-center justify-between">
            <div>
              <p class="font-label-sm text-label-sm text-on-surface">Nur Pro-Benutzer</p>
              <p class="text-body-sm text-on-surface-variant">Beschränkt auf Premium-Mitglieder</p>
            </div>
            <label class="relative inline-flex items-center cursor-pointer">
              <input type="checkbox" id="pro-filter" class="sr-only peer" />
              <div class="w-11 h-6 rounded-full peer bg-surface-container-highest border border-outline/20 peer-checked:bg-primary peer-checked:border-primary transition-colors after:content-[''] after:absolute after:top-0.5 after:left-0.5 after:bg-white after:border after:border-outline/20 after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:after:translate-x-5"></div>
            </label>
          </div>

          <!-- Submit -->
          <div class="flex items-center gap-md pt-sm border-t border-outline/20">
            <button
              type="submit"
              id="send-btn"
              class="font-nav-item text-nav-item uppercase tracking-widest px-8 py-3 rounded-full min-h-11 transition-colors bg-primary text-on-primary hover:opacity-90 disabled:opacity-50 disabled:cursor-not-allowed"
            >
              SENDEN
            </button>
            <span id="feedback" class="text-body-sm text-on-surface-variant hidden"></span>
          </div>
        </form>
      </div>

      <!-- History -->
      <div class="mt-xl">
        <h2 class="text-headline-md text-on-surface mb-lg">Verlauf</h2>
        <div id="history-loading" class="text-body-md text-on-surface-variant">Lade…</div>
        <div id="history-list" class="hidden space-y-md"></div>
        <div id="history-empty" class="hidden text-body-md text-on-surface-variant">Noch keine Benachrichtigungen gesendet.</div>
      </div>
    </div>
  </main>

  <script>
    document.addEventListener("DOMContentLoaded", async () => {
      const form = document.getElementById("notify-form")!;
      const titleInput = document.getElementById("title") as HTMLInputElement;
      const bodyInput = document.getElementById("body") as HTMLTextAreaElement;
      const roleSelect = document.getElementById("role-filter") as HTMLSelectElement;
      const proCheckbox = document.getElementById("pro-filter") as HTMLInputElement;
      const sendBtn = document.getElementById("send-btn") as HTMLButtonElement;
      const feedbackEl = document.getElementById("feedback")!;

      // ── Send ──────────────────────────────────────
      form.addEventListener("submit", async (e) => {
        e.preventDefault();

        sendBtn.disabled = true;
        sendBtn.textContent = "SENDE…";
        feedbackEl.classList.add("hidden");

        const filter: { role?: string; is_pro?: boolean } = {};
        if (roleSelect.value) filter.role = roleSelect.value;
        if (proCheckbox.checked) filter.is_pro = true;
        const filterPayload = Object.keys(filter).length > 0 ? filter : null;

        try {
          const res = await fetch("/api/admin/notifications", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              title: titleInput.value.trim(),
              body: bodyInput.value.trim(),
              filter: filterPayload,
            }),
          });

          const data = await res.json();

          if (res.ok) {
            feedbackEl.textContent =
              "Gesendet (" + data.sent + " Push" + (data.expired ? ", " + data.expired + " abgelaufen" : "") + ")" +
              (data.warning ? " — " + data.warning : "");
            feedbackEl.className = "text-body-sm text-green-600";
            titleInput.value = "";
            bodyInput.value = "";
            roleSelect.value = "";
            proCheckbox.checked = false;
            loadHistory();
          } else {
            feedbackEl.textContent = "Fehler: " + (data.error || "Unbekannt");
            feedbackEl.className = "text-body-sm text-error";
          }
        } catch {
          feedbackEl.textContent = "Fehler beim Senden.";
          feedbackEl.className = "text-body-sm text-error";
        }

        feedbackEl.classList.remove("hidden");
        sendBtn.disabled = false;
        sendBtn.textContent = "SENDEN";
      });

      // ── History ────────────────────────────────────
      const historyLoading = document.getElementById("history-loading")!;
      const historyList = document.getElementById("history-list")!;
      const historyEmpty = document.getElementById("history-empty")!;

      async function loadHistory() {
        try {
          const res = await fetch("/api/admin/notifications?limit=20");
          if (!res.ok) {
            historyLoading.textContent = "Fehler beim Laden.";
            return;
          }
          const data = await res.json();
          renderHistory(data.notifications ?? []);
        } catch {
          historyLoading.textContent = "Fehler beim Laden.";
        }
      }

      function renderHistory(notifications: any[]) {
        historyLoading.classList.add("hidden");

        if (notifications.length === 0) {
          historyEmpty.classList.remove("hidden");
          historyList.classList.add("hidden");
          return;
        }

        historyEmpty.classList.add("hidden");
        historyList.classList.remove("hidden");

        historyList.innerHTML = notifications.map((n: any) => {
          const filterParts: string[] = [];
          if (n.filter?.role) filterParts.push("Rolle: " + n.filter.role);
          if (n.filter?.is_pro) filterParts.push("Pro");

          return (
            '<div class="bg-surface-container-lowest rounded-xl p-md border border-outline/20">' +
              '<div class="flex items-start justify-between gap-sm">' +
                '<div class="flex-1 min-w-0">' +
                  '<p class="font-label-sm text-label-sm text-on-surface truncate">' + escHtml(n.title) + '</p>' +
                  '<p class="text-body-sm text-on-surface-variant mt-xs line-clamp-2">' + escHtml(n.body) + '</p>' +
                '</div>' +
              '</div>' +
              '<div class="flex items-center gap-sm mt-xs">' +
                '<span class="text-caption text-on-surface-variant">' + formatDate(n.sent_at || n.created_at) + '</span>' +
                (filterParts.length > 0 ? '<span class="text-caption text-primary">' + filterParts.join(" · ") + '</span>' : '<span class="text-caption text-on-surface-variant">Alle</span>') +
              '</div>' +
            '</div>'
          );
        }).join("");
      }

      function escHtml(s: string): string {
        const div = document.createElement("div");
        div.textContent = s;
        return div.innerHTML;
      }

      function formatDate(iso: string): string {
        if (!iso) return "";
        return new Date(iso).toLocaleString("de-DE", {
          day: "2-digit",
          month: "2-digit",
          year: "numeric",
          hour: "2-digit",
          minute: "2-digit",
        });
      }

      loadHistory();
    });
  </script>
</Layout>