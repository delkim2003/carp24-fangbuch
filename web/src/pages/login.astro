---
import Layout from "../layouts/Layout.astro";
import { t } from "../lib/i18n";
---

<Layout title="Carp24 – Anmelden">
  <main class="flex-grow flex items-center justify-center p-margin-mobile md:p-margin-desktop py-xl">
    <div class="bg-surface-container-high border border-primary/40 rounded-2xl w-full max-w-md p-lg md:p-xl mx-auto flex flex-col gap-lg">
      <div class="text-center mb-sm">
        <h1 class="text-headline-lg-mobile md:text-headline-lg text-on-surface mb-2 i18n" data-de={t("login.willkommen", "de")} data-en={t("login.willkommen", "en")}>{t("login.willkommen")}</h1>
        <p class="text-body-md text-on-surface-variant i18n" data-de={t("login.subtitle", "de")} data-en={t("login.subtitle", "en")}>{t("login.subtitle")}</p>
      </div>

      <div id="login-error" class="hidden p-md bg-error-container text-on-error-container rounded-lg text-body-md" role="alert" aria-live="polite"></div>
      <div id="login-success" class="hidden p-md bg-primary-container/20 text-primary rounded-lg text-body-md"></div>

      <form id="login-form" class="flex flex-col gap-md">
        <div class="flex flex-col gap-xs">
          <label class="font-label-sm text-label-sm text-secondary uppercase tracking-widest i18n" for="email" data-de={t("login.email", "de")} data-en={t("login.email", "en")}>{t("login.email")}</label>
          <input class="w-full bg-surface-container-lowest rounded-lg px-md py-3 text-on-surface border border-primary/20 focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary transition-colors min-h-12" id="email" name="email" type="email" placeholder="angler@example.com" autocomplete="email" required />
        </div>
        <div class="flex flex-col gap-xs">
          <label class="font-label-sm text-label-sm text-secondary uppercase tracking-widest i18n" for="password" data-de={t("login.passwort", "de")} data-en={t("login.passwort", "en")}>{t("login.passwort")}</label>
          <input class="w-full bg-surface-container-lowest rounded-lg px-md py-3 text-on-surface border border-primary/20 focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary transition-colors min-h-12" id="password" name="password" type="password" placeholder="••••••••" autocomplete="current-password" required />
        </div>
        <div class="mt-sm mb-sm"></div>
        <button type="submit" id="login-btn" class="w-full bg-primary-container text-on-primary-container rounded-full py-3 px-lg font-nav-item text-nav-item uppercase tracking-widest hover:bg-surface-tint transition-all min-h-12 mt-sm disabled:opacity-50 disabled:cursor-not-allowed i18n" data-de={t("login.anmelden", "de")} data-en={t("login.anmelden", "en")}>
          {t("login.anmelden")}
        </button>
      </form>

      <p class="text-center mt-4">
        <a href="/passwort-vergessen" class="text-primary hover:underline text-sm i18n" data-de={t("login.passwort_vergessen", "de")} data-en={t("login.passwort_vergessen", "en")}>{t("login.passwort_vergessen")}</a>
      </p>

      <div class="flex items-center gap-sm my-xs">
        <div class="flex-grow border-t border-primary/20"></div>
        <span class="text-body-sm text-on-surface-variant uppercase tracking-widest i18n" data-de={t("login.oder", "de")} data-en={t("login.oder", "en")}>{t("login.oder")}</span>
        <div class="flex-grow border-t border-primary/20"></div>
      </div>

      <div id="oauth-error" class="hidden p-md bg-error-container text-on-error-container rounded-lg text-body-md" role="alert" aria-live="polite"></div>

      <div class="flex flex-col sm:flex-row gap-sm">
        <button type="button" id="oauth-google-btn" class="flex-1 flex items-center justify-center gap-sm border border-primary/40 text-on-surface rounded-full py-3 px-lg font-nav-item text-nav-item uppercase tracking-widest hover:bg-surface-container-high transition-all min-h-12 disabled:opacity-50 disabled:cursor-not-allowed">
          <span class="font-bold text-lg">G</span> Google
        </button>
        <button type="button" id="oauth-facebook-btn" class="flex-1 flex items-center justify-center gap-sm border border-primary/40 text-on-surface rounded-full py-3 px-lg font-nav-item text-nav-item uppercase tracking-widest hover:bg-surface-container-high transition-all min-h-12 disabled:opacity-50 disabled:cursor-not-allowed">
          <span class="font-bold text-lg">f</span> Facebook
        </button>
      </div>

      <div id="totp-screen" class="hidden flex flex-col gap-md">
        <div class="text-center mb-sm">
          <h2 class="text-headline-lg-mobile md:text-headline-lg text-on-surface mb-2 i18n" data-de={t("login.totp_titel", "de")} data-en={t("login.totp_titel", "en")}>{t("login.totp_titel")}</h2>
          <p class="text-body-md text-on-surface-variant i18n" data-de={t("login.totp_subtitle", "de")} data-en={t("login.totp_subtitle", "en")}>{t("login.totp_subtitle")}</p>
        </div>
        <div id="totp-error" class="hidden p-md bg-error-container text-on-error-container rounded-lg text-body-md" role="alert" aria-live="polite"></div>
        <div class="flex flex-col gap-xs">
          <label class="font-label-sm text-label-sm text-secondary uppercase tracking-widest i18n" for="totp-code" data-de={t("login.totp_code", "de")} data-en={t("login.totp_code", "en")}>{t("login.totp_code")}</label>
          <input class="w-full bg-surface-container-lowest rounded-lg px-md py-3 text-on-surface border border-primary/20 focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary transition-colors min-h-12 text-center tracking-[0.5em] font-mono text-lg" id="totp-code" name="totp-code" type="text" inputmode="numeric" pattern="[0-9]{6}" maxlength="6" placeholder="000000" autocomplete="one-time-code" required />
        </div>
        <button type="button" id="totp-btn" class="w-full bg-primary-container text-on-primary-container rounded-full py-3 px-lg font-nav-item text-nav-item uppercase tracking-widest hover:bg-surface-tint transition-all min-h-12 mt-sm disabled:opacity-50 disabled:cursor-not-allowed i18n" data-de={t("login.totp_bestaetigen", "de")} data-en={t("login.totp_bestaetigen", "en")}>
          {t("login.totp_bestaetigen")}
        </button>
        <button type="button" id="totp-cancel-btn" class="w-full border border-primary/40 text-on-surface rounded-full py-3 px-lg font-nav-item text-nav-item uppercase tracking-widest hover:bg-surface-container-high transition-all min-h-12 i18n" data-de={t("login.totp_abbrechen", "de")} data-en={t("login.totp_abbrechen", "en")}>
          {t("login.totp_abbrechen")}
        </button>
      </div>

      <div class="border-t border-primary/20 pt-md mt-sm">
        <p class="text-body-md text-on-surface-variant text-center mb-md i18n" data-de={t("login.kein_konto", "de")} data-en={t("login.kein_konto", "en")}>{t("login.kein_konto")}</p>
        <form id="register-form" class="flex flex-col gap-md">
          <div class="flex flex-col gap-xs">
            <label class="font-label-sm text-label-sm text-secondary uppercase tracking-widest" for="reg-email">{t("login.email")}</label>
            <input class="w-full bg-surface-container-lowest rounded-lg px-md py-3 text-on-surface border border-primary/20 focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary transition-colors min-h-12" id="reg-email" type="email" placeholder="angler@example.com" autocomplete="email" required />
          </div>
          <div class="flex flex-col gap-xs">
            <label class="font-label-sm text-label-sm text-secondary uppercase tracking-widest" for="reg-password">{t("login.passwort")}</label>
            <input class="w-full bg-surface-container-lowest rounded-lg px-md py-3 text-on-surface border border-primary/20 focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary transition-colors min-h-12" id="reg-password" type="password" placeholder="••••••••" autocomplete="new-password" minlength="8" required />
          </div>
          <div class="flex flex-col gap-xs">
            <label class="font-label-sm text-label-sm text-secondary uppercase tracking-widest i18n" for="reg-geburtsdatum" data-de={t("login.geburtsdatum", "de")} data-en={t("login.geburtsdatum", "en")}>{t("login.geburtsdatum")}</label>
            <input class="w-full bg-surface-container-lowest rounded-lg px-md py-3 text-on-surface border border-primary/20 focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary transition-colors min-h-12" id="reg-geburtsdatum" type="date" />
            <p class="text-body-sm text-on-surface-variant i18n" data-de={t("login.geburtsdatum_hint", "de")} data-en={t("login.geburtsdatum_hint", "en")}>{t("login.geburtsdatum_hint")}</p>
          </div>
          <div class="flex flex-col gap-xs">
            <label class="font-label-sm text-label-sm text-secondary uppercase tracking-widest i18n" for="reg-land" data-de={t("login.land", "de")} data-en={t("login.land", "en")}>{t("login.land")}</label>
            <select class="w-full bg-surface-container-lowest rounded-lg px-md py-3 text-on-surface border border-primary/20 focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary transition-colors min-h-12 cursor-pointer" id="reg-land">
              <option value="" data-de={t("login.land_placeholder", "de")} data-en={t("login.land_placeholder", "en")}>{t("login.land_placeholder")}</option>
              <option value="AT">Österreich</option>
              <option value="DE">Deutschland</option>
              <option value="CH">Schweiz</option>
              <option value="HU">Ungarn</option>
              <option value="CZ">Tschechien</option>
              <option value="SK">Slowakei</option>
              <option value="PL">Polen</option>
              <option value="HR">Kroatien</option>
              <option value="SI">Slowenien</option>
              <option value="IT">Italien</option>
              <option value="NL">Niederlande</option>
              <option value="BE">Belgien</option>
              <option value="FR">Frankreich</option>
              <option value="GB">Großbritannien</option>
              <option value="OTHER">Sonstiges</option>
            </select>
          </div>
          <div class="flex flex-col gap-xs">
            <label class="font-label-sm text-label-sm text-secondary uppercase tracking-widest i18n" for="reg-gewaesser" data-de={t("login.heimatgewaesser", "de")} data-en={t("login.heimatgewaesser", "en")}>{t("login.heimatgewaesser")}</label>
            <input class="w-full bg-surface-container-lowest rounded-lg px-md py-3 text-on-surface border border-primary/20 focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary transition-colors min-h-12" id="reg-gewaesser" type="text" placeholder={t("login.heimatgewaesser_placeholder")} />
          </div>
          <div class="flex items-start gap-sm">
            <input type="checkbox" id="reg-datenschutz" class="mt-1 w-4 h-4 rounded border-primary/40 text-primary focus:ring-primary cursor-pointer" required />
            <label for="reg-datenschutz" class="text-body-sm text-on-surface-variant cursor-pointer">
              <span class="i18n" data-de={t("login.datenschutz", "de")} data-en={t("login.datenschutz", "en")}>{t("login.datenschutz")}</span>
              <a href="/datenschutz" class="text-primary hover:underline i18n" data-de={t("login.datenschutz_link", "de")} data-en={t("login.datenschutz_link", "en")}>{t("login.datenschutz_link")}</a>
            </label>
          </div>
          <div id="turnstile-container" class="flex justify-center"></div>
          <button type="submit" id="register-btn" class="w-full border border-primary/40 text-on-surface rounded-full py-3 px-lg font-nav-item text-nav-item uppercase tracking-widest hover:bg-surface-container-high transition-all min-h-12 disabled:opacity-50 disabled:cursor-not-allowed i18n" data-de={t("login.registrieren", "de")} data-en={t("login.registrieren", "en")}>
            {t("login.registrieren")}
          </button>
        </form>
        <p id="reg-hint" class="hidden text-body-md text-secondary text-center mt-sm" role="status" aria-live="polite"></p>
      </div>
    </div>
  </main>
</Layout>

<script is:inline>
(function () {
  var SUPABASE_URL = document.body.dataset.supabaseUrl || "";
  var SUPABASE_ANON_KEY = document.body.dataset.supabaseAnonKey || "";

  function getSupabase() {
    if (window.__supabase) return window.__supabase;
    if (typeof window.supabase !== 'undefined' && window.supabase.createClient) {
      window.__supabase = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY, {
        auth: { persistSession: true, autoRefreshToken: true, detectSessionInUrl: true, storageKey: "sb-carp24-auth-token" },
        cookieOptions: { path: '/', sameSite: 'lax', secure: window.location.protocol === 'https:' }
      });
      return window.__supabase;
    }
    return null;
  }

  var loginForm = document.getElementById('login-form');
  var registerForm = document.getElementById('register-form');
  var loginError = document.getElementById('login-error');
  var loginSuccess = document.getElementById('login-success');
  var loginBtn = document.getElementById('login-btn');
  var registerBtn = document.getElementById('register-btn');
  var regHint = document.getElementById('reg-hint');
  var totpScreen = document.getElementById('totp-screen');
  var totpError = document.getElementById('totp-error');
  var totpBtn = document.getElementById('totp-btn');
  var totpCancelBtn = document.getElementById('totp-cancel-btn');
  var totpCodeInput = document.getElementById('totp-code');

  var pendingFactorId = null;

  loginForm.addEventListener('submit', function (e) {
    e.preventDefault();
    hideMessages();
    var email = document.getElementById('email').value.trim();
    var password = document.getElementById('password').value;
    if (!email || !password) {
      showError('Bitte E-Mail und Passwort eingeben.');
      return;
    }
    loginBtn.disabled = true;
    loginBtn.textContent = 'WIRD ANGEMELDET…';
    var sb = getSupabase();
    if (!sb) {
      showError('Supabase nicht initialisiert.');
      loginBtn.disabled = false;
      loginBtn.textContent = 'ANMELDEN';
      return;
    }
    sb.auth.signInWithPassword({ email: email, password: password }).then(function (res) {
      if (res.error) {
        loginBtn.disabled = false;
        loginBtn.textContent = 'ANMELDEN';
        showError('Ungültige Anmeldedaten. Bitte überprüfe E-Mail und Passwort.');
        return;
      }
      sb.auth.mfa.getAuthenticatorAssuranceLevel().then(function (aalRes) {
        var currentLevel = aalRes.data && aalRes.data.currentLevel;
        var nextLevel = aalRes.data && aalRes.data.nextLevel;
        if (currentLevel === 'aal1' && nextLevel === 'aal2') {
          sb.auth.mfa.listFactors().then(function (factorsRes) {
            var factors = factorsRes.data && factorsRes.data.totp ? factorsRes.data.totp : [];
            var verified = null;
            for (var i = 0; i < factors.length; i++) {
              if (factors[i].status === 'verified') { verified = factors[i]; break; }
            }
            if (verified) {
              pendingFactorId = verified.id;
              loginForm.classList.add('hidden');
              totpScreen.classList.remove('hidden');
              totpScreen.classList.add('flex');
              totpCodeInput.focus();
              loginBtn.disabled = false;
              loginBtn.textContent = 'ANMELDEN';
            } else {
              window.location.href = '/fang-erfassen';
            }
          });
        } else {
          window.location.href = '/fang-erfassen';
        }
      }).catch(function () {
        window.location.href = '/fang-erfassen';
      });
    });
  });

  totpBtn.addEventListener('click', function () {
    var code = totpCodeInput.value.trim();
    if (!code || code.length !== 6 || !/^[0-9]{6}$/.test(code)) {
      showTotpError('Bitte gib einen 6-stelligen Code ein.');
      return;
    }
    if (!pendingFactorId) {
      showTotpError('Kein Faktor ausgewählt. Bitte erneut anmelden.');
      return;
    }
    totpBtn.disabled = true;
    totpBtn.textContent = 'WIRD BESTÄTIGT…';
    var sb = getSupabase();
    if (!sb) {
      showTotpError('Supabase nicht initialisiert.');
      totpBtn.disabled = false;
      totpBtn.textContent = 'BESTÄTIGEN';
      return;
    }
    sb.auth.mfa.challengeAndVerify({ factorId: pendingFactorId, code: code }).then(function (res) {
      totpBtn.disabled = false;
      totpBtn.textContent = 'BESTÄTIGEN';
      if (res.error) {
        showTotpError('Code ungültig oder abgelaufen. Bitte erneut versuchen.');
        totpCodeInput.value = '';
        totpCodeInput.focus();
      } else {
        window.location.href = '/fang-erfassen';
      }
    });
  });

  totpCodeInput.addEventListener('keydown', function (e) {
    if (e.key === 'Enter') {
      e.preventDefault();
      totpBtn.click();
    }
  });

  totpCancelBtn.addEventListener('click', function () {
    pendingFactorId = null;
    totpCodeInput.value = '';
    hideTotpError();
    totpScreen.classList.add('hidden');
    totpScreen.classList.remove('flex');
    loginForm.classList.remove('hidden');
    var sb = getSupabase();
    if (sb) sb.auth.signOut();
  });

  var oauthGoogleBtn = document.getElementById('oauth-google-btn');
  var oauthFacebookBtn = document.getElementById('oauth-facebook-btn');
  var oauthError = document.getElementById('oauth-error');

  function showOAuthError(msg) {
    oauthError.textContent = msg;
    oauthError.classList.remove('hidden');
  }

  function getSupabaseOrShowOAuthError() {
    var sb = getSupabase();
    if (!sb) {
      showOAuthError('Supabase nicht initialisiert.');
      return null;
    }
    return sb;
  }

  oauthGoogleBtn.addEventListener('click', function () {
    hideMessages();
    oauthError.classList.add('hidden');
    var sb = getSupabaseOrShowOAuthError();
    if (!sb) return;
    sb.auth.signInWithOAuth({
      provider: 'google',
      options: { redirectTo: window.location.origin }
    }).then(function (res) {
      if (res.error) {
        var msg = res.error.message || '';
        if (/unsupported.*provider|provider.*not.*enabled|invalid.*provider/i.test(msg)) {
          showOAuthError('Google-Login ist noch nicht aktiviert — bald verfügbar.');
        } else {
          showOAuthError('Anmeldung mit Google fehlgeschlagen. Bitte versuche es später erneut.');
        }
      }
    });
  });

  oauthFacebookBtn.addEventListener('click', function () {
    hideMessages();
    oauthError.classList.add('hidden');
    var sb = getSupabaseOrShowOAuthError();
    if (!sb) return;
    sb.auth.signInWithOAuth({
      provider: 'facebook',
      options: { redirectTo: window.location.origin }
    }).then(function (res) {
      if (res.error) {
        var msg = res.error.message || '';
        if (/unsupported.*provider|provider.*not.*enabled|invalid.*provider/i.test(msg)) {
          showOAuthError('Facebook-Login ist noch nicht aktiviert — bald verfügbar.');
        } else {
          showOAuthError('Anmeldung mit Facebook fehlgeschlagen. Bitte versuche es später erneut.');
        }
      }
    });
  });

  var TURNSTILE_SITEKEY = 'PLACEHOLDER_TURNSTILE_SITEKEY'; // Wird nach Cloudflare-Setup ersetzt
  var turnstileToken = null;

  // Turnstile callback
  window.onTurnstileSuccess = function(token) {
    turnstileToken = token;
  };

  // Load Turnstile script
  if (TURNSTILE_SITEKEY !== 'PLACEHOLDER_TURNSTILE_SITEKEY') {
    var ts = document.createElement('script');
    ts.src = 'https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit';
    ts.async = true;
    ts.defer = true;
    ts.onload = function() {
      var container = document.getElementById('turnstile-container');
      if (container && window.turnstile) {
        window.turnstile.render(container, {
          sitekey: TURNSTILE_SITEKEY,
          callback: function(token) { turnstileToken = token; },
          'error-callback': function() { turnstileToken = null; }
        });
      }
    };
    document.head.appendChild(ts);
  }

  registerForm.addEventListener('submit', function (e) {
    e.preventDefault();
    hideMessages();
    var email = document.getElementById('reg-email').value.trim();
    var password = document.getElementById('reg-password').value;
    var datenschutz = document.getElementById('reg-datenschutz').checked;
    if (!email || !password) {
      showError('Bitte E-Mail und Passwort eingeben.');
      return;
    }
    if (password.length < 8) {
      showError('Passwort muss mindestens 8 Zeichen lang sein.');
      return;
    }
    if (!datenschutz) {
      showError('Bitte akzeptiere die Datenschutzerklärung.');
      return;
    }
    registerBtn.disabled = true;
    registerBtn.textContent = 'WIRD REGISTRIERT…';
    var sb = getSupabase();
    if (!sb) {
      showError('Supabase nicht initialisiert.');
      registerBtn.disabled = false;
      registerBtn.textContent = 'REGISTRIEREN';
      return;
    }
    // Collect optional profile data
    var geburtsdatum = document.getElementById('reg-geburtsdatum').value;
    var land = document.getElementById('reg-land').value;
    var gewaesser = document.getElementById('reg-gewaesser').value.trim();
    var metadata = {};
    if (geburtsdatum) metadata.geburtsdatum = geburtsdatum;
    if (land) metadata.land = land;
    if (gewaesser) metadata.heimatgewaesser = gewaesser;

    var signUpOpts = { email: email, password: password, options: { data: metadata } };
    if (turnstileToken) signUpOpts.options.captchaToken = turnstileToken;

    sb.auth.signUp(signUpOpts).then(function (res) {
      registerBtn.disabled = false;
      registerBtn.textContent = 'REGISTRIEREN';
      if (res.error) {
        showError('Registrierung fehlgeschlagen: ' + res.error.message);
        // Reset turnstile
        if (window.turnstile) window.turnstile.reset();
        turnstileToken = null;
      } else if (res.data.user && res.data.user.identities && res.data.user.identities.length === 0) {
        showError('Diese E-Mail ist bereits registriert.');
      } else {
        regHint.textContent = 'Registrierung erfolgreich! Bitte bestätige deine E-Mail-Adresse über den Link, den wir dir geschickt haben.';
        regHint.classList.remove('hidden');
        regHint.classList.add('text-primary');
        registerForm.classList.add('hidden');
      }
    });
  });

  function showError(msg) {
    loginError.textContent = msg;
    loginError.classList.remove('hidden');
  }

  function showTotpError(msg) {
    totpError.textContent = msg;
    totpError.classList.remove('hidden');
  }

  function hideTotpError() {
    totpError.classList.add('hidden');
  }

  function hideMessages() {
    loginError.classList.add('hidden');
    loginSuccess.classList.add('hidden');
    regHint.classList.add('hidden');
    oauthError.classList.add('hidden');
    hideTotpError();
  }
})();
</script>
